namespace Wordle
{
    class Program
    {
        static void Main(string[] args)
        {
            // Declare Objects.
            Model model = new Model();
            // Game Loop?
            // model.RunGame();
            model.RunGame();
        }
    }
}